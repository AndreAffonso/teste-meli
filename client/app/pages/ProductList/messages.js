import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProductList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProductList component!',
  },
});
